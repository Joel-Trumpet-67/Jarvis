SECRET_KEY=change-me-to-a-random-string

# Which AI backend Jarvis talks to: anthropic | groq | ollama | openai
AI_PROVIDER=groq
# Optional override; each provider has a sensible default model if omitted.
AI_MODEL=

# Needed only when AI_PROVIDER=groq
GROQ_API_KEY=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

# Needed only when AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Needed only when AI_PROVIDER=openai
OPENAI_API_KEY=sk-...

# Needed only when AI_PROVIDER=ollama (defaults to a local install)
OLLAMA_BASE_URL=http://localhost:11434/v1

JOEL_PASSWORD=change-me
VALERIE_PASSWORD=change-me

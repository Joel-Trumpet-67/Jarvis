[Unit]
Description=Jarvis assistant server
After=network.target

[Service]
Type=simple
User=jarvis
WorkingDirectory=/opt/jarvis
Environment=DEBUG=false
ExecStart=/opt/jarvis/.venv/bin/gunicorn -w 2 -b 127.0.0.1:5050 run:app
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
